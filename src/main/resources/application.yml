spring:
  profiles:
    active: dev
---
spring:
  profiles: dev
  data:
    mongodb:
      port: 0
vcah:
  deck:
    black: default-black-deck.txt
    white: default-white-deck.txt
---
spring:
  profiles: test
  data:
    mongodb:
      port: 0
vcah:
  deck:
    default:
      filenames: 